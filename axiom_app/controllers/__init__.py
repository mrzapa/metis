"""axiom_app.controllers — Application logic that mediates between models and views.

Planned contents (to be migrated from agentic_rag_gui.py):
  - IngestionController  (file loading, chunking, embedding, indexing)
  - ChatController       (query dispatch, agentic loop, streaming)
  - SessionController    (history, sessions DB, export)
  - SettingsController   (config load/save, provider switching)
"""
