"""metis_app.controllers — Application logic that mediates between models and views.

The current MVC runtime centralizes controller behavior in ``AppController``,
including indexing, chat dispatch, settings persistence, history/session
management, evidence rendering, and startup/bootstrap coordination.
"""
